# Shared Module

This directory contains shared types, constants, data schemas, and contracts utilized across both the **frontend** (Next.js) and **backend** (NestJS) applications.

## Directory Layout
- `/types`: Shared TypeScript interfaces and DTOs.
- `/constants`: Shared application-wide constants and enums.
- `/utils`: Common utility functions shared across frontend and backend.
